<h1>Listing certificates</h1>

<table>
  <thead>
    <tr>
      <th>Identifier</th>
      <th>Shares info</th>
      <th>Share holder</th>
      <th>Certificate date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @certificates.each do |certificate| %>
      <tr>
        <td><%= certificate.identifier %></td>
        <td><%= certificate.shares_info %></td>
        <td><%= certificate.share_holder %></td>
        <td><%= certificate.certificate_date %></td>
        <td><%= link_to 'Show', certificate %></td>
        <td><%= link_to 'Edit', edit_certificate_path(certificate) %></td>
        <td><%= link_to 'Destroy', certificate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Certificate', new_certificate_path %>
